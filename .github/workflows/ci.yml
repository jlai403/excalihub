name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      - run: bun run typecheck
      - run: bun test server/tests/

  e2e:
    needs: test
    runs-on: ubuntu-latest
    concurrency:
      group: ci-e2e
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      - run: bunx playwright install --with-deps
      # test:e2e:docker builds the image (in globalSetup) and runs the suite
      # against the container, so it doubles as the image build gate.
      - run: bun run test:e2e:docker
        env:
          E2E_GIT_REPO: jlai403/excalihub-ci
          E2E_SSH_PRIVATE_KEY: ${{ secrets.E2E_SSH_PRIVATE_KEY }}
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: playwright-report
          path: tests/e2e/test-results/

  lint:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: immanuwell/dockerfile-roast@1.4.7
        with:
          files: Dockerfile
          min-severity: info
